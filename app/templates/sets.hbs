{{#link-to "sets.create" class="btn btn-primary create-btn"}} Add set {{/link-to}}

<div class="row">
  <div class="col-sm-3">
    {{#each set in controller}}
      <p>
        {{#link-to "set" set}}{{set.date}}{{/link-to}}
      </p>
    {{/each}}
  </div>

  <div class="col-sm-9">
    {{outlet}}
  </div>
</div>
